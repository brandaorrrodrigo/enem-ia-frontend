/**
 * BIBLIOTECA DE CONTEÚDO - VERSÃO LAZY LOADING
 * Carrega conteúdo sob demanda para evitar crashes no build
 */

export async function getConteudoModulo(materiaId: string, moduloSlug: string) {
  // Implementar lazy loading de conteúdo
  // Por enquanto retorna vazio
  return null;
}

// Exportar objeto vazio para compatibilidade
export const bibliotecaConteudo = {};
